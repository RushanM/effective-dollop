# Экшен для выпуска релизных версий набора ресурсов на Modrinth и CurseForge

name: Выпустить релиз на сайты

on:
  workflow_dispatch:

jobs:
  build_and_release:
    name: Сборка и выпуск
    runs-on: ubuntu-latest